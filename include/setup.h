#ifndef SETUP_H
#define SETUP_H
#include <start_game.h>

class setup
{
    public:
        setup(int &score, int &lives, int pcd[], int &pass, int &v, double &dif, int &rscore, int &gp);
        char text[50];

    protected:

    private:
};

#endif // SETUP_H
